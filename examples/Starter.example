import java.io.IOException;
// Point, Triangle, Mesh, STL
import printjava.*;
// Rect, Sphere, Graph, Field, etc.
import printjava.Meshes.*;

public class Main {
    public static void main(String[] args) throws IOException {
        // Will create a new STL file called "model.stl"
        STL f = new STL("model");

        // This is where you can add meshes to the STL file
        // See other examples for more info on how to add meshes

        // Creates the file
        f.write();
    }
}